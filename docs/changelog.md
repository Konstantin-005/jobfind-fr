## [2026-04-09] - Поле «Ученический договор» (StudentContract) в вакансиях
### Добавлено
- Обработка и отправка поля `student_contract` в `VacancyForm.tsx` для поддержки ученического договора при создании и редактировании вакансий.

### Изменено
- Типы и состояние формы расширены для поддержки нового поля.

### Исправлено
- Нет

## [2026-01-07] - Публичные страницы компаний
### Добавлено
- SSR-страница списка компаний `/companies` с фильтрами по отраслям и поиском по названию, lazy load с кнопкой «Показать ещё»
- SSR-страница компании `/companies/{id}` с вкладками «Вакансии» и «Инфо» и отдельной 404-страницей «Компания не найдена»

### Изменено
- Подключение словаря форматов работы к публичным вакансиям компании через `work_format_ids`
 - Логика SSR-загрузки отраслей и списка компаний на странице `/companies` переведена на использование `headers()` и заголовков `x-forwarded-proto`/`x-forwarded-host` для построения абсолютных URL (по аналогии с страницей вакансии)
 - Карточки вакансий во вкладке «Вакансии» на странице компании приведены к тому же визуальному оформлению, что и карточки в списке вакансий (`VacancyClient`)
 - Из карточек вакансий во вкладке «Вакансии» на странице компании убран логотип компании (сохранены заголовок, зарплата, метки и адрес)
 - Вкладки «Вакансии»/«Инфо» на странице компании визуально приближены к макету: вынесены в отдельный белый блок под карточкой компании, контент вкладок отображается в отдельных белых блоках на сером фоне без изменения SSR/CSR-логики
 - Клиентский компонент вкладок `CompanyTabs` больше не используется; контент вкладок перенесён на SSR-страницу компании, описание компании частично показано в шапке с возможностью раскрытия полного текста
 - На странице компании названия вакансий стали ссылками на публичную страницу вакансии `/vacancy/{id}`

### Исправлено
- Неверная обработка ответа `/api/dictionaries/companies` на странице `/companies` (ожидался объект с полем `data`, тогда как бэкенд возвращает массив компаний); теперь и SSR-функция `fetchInitialData`, и клиентский `CompaniesClient` поддерживают оба варианта структуры ответа
- Ошибка `Failed to parse URL` при загрузке компании на странице `/companies/[id]` из-за относительного URL в `fetch`; запрос переписан на использование абсолютного URL через `headers()` и заголовки `x-forwarded-proto`/`x-forwarded-host`
- Runtime-ошибка `Failed to load company` на странице `/companies/{id}` теперь обрабатывается через отдельный экран состояния «Компания недоступна» вместо падения страницы
- TypeScript-ошибка в `CompanyTabs` (обращение к `company` вне области видимости) устранена; для подписи и alt-атрибутов используется `job.company_name`


## [2026-01-31] - Пагинация вакансий по slug
### Добавлено
- Нет

### Изменено
- SSR-страница `/vakansii/[slug]` адаптирована под новый формат ответа `/api/jobs/searchBySlug` (`items`, `total`, `page`, `limit`)

### Исправлено
- Восстановлен вывод пагинации на странице вакансий по городу при наличии нескольких страниц


## [2026-02-14] - Пагинация списка вакансий
### Добавлено
- Нет

### Изменено
- Клиентская страница списка вакансий (`VacancyClient`) адаптирована под новый формат ответа `GET /api/jobs` (`items`, `total`, `page`, `limit`)
- Для страниц вакансий с параметром `?page` добавлены SEO-настройки: canonical на базовую страницу и запрет индексации для `page > 1`

### Исправлено
- Восстановлен корректный расчёт количества страниц и отображение пагинации в списке вакансий


## [2026-02-15] - Каталог компаний: пагинация и SEO noindex
### Добавлено
- Запрет индексации (robots + Yandex) для страниц каталога компаний `/companies` при наличии `query` и/или `page > 1`

### Изменено
- Клиентская часть каталога компаний переведена на серверную пагинацию через `page`/`limit` вместо lazy load «Показать ещё»
- Запрос `GET /api/dictionaries/companies` на фронтенде адаптирован под новый формат ответа (`items`, `total`, `page`, `limit`) с обратной совместимостью
- Во фронтовом каталоге компаний оставлен только фильтр по отраслям (`industry_id[]`) и поиск по названию (`query`)

### Исправлено
- Нормализация ответа каталога компаний унифицирована между SSR-первичной загрузкой и клиентскими запросами


## [2026-02-15] - Ссылки на страницы компаний из вакансий
### Добавлено
- Ссылки на страницы компаний `/companies/{company_id}` из списка вакансий `/vacancy`, списка вакансий по городу `/vakansii/[slug]` и детальной вакансии `/vacancy/[id]`

### Изменено
- Нет

### Исправлено
- Нет


## [2026-01-06] - Отклики: кнопки статусов и отказа
### Добавлено
- Открытие модалки смены статуса при нажатии «Отказать» для любых статусов отклика

### Изменено
- Кнопка «Изменить статус» скрывается для откликов в статусе «Выход на работу» и «Отказ»

### Исправлено
- Блокировка открытия модалки при принудительном выборе статуса «Отказ» устранена


## [2026-01-06] - Модалка статусов: согласование статуса и текста
### Добавлено
- Автовыбор первого допустимого следующего статуса при открытии модалки смены статуса отклика

### Изменено
- deriveInitialStatus теперь возвращает первый разрешённый статус, чтобы значение селекта совпадало с вариантами переходов

### Исправлено
- Несоответствие: селект показывал «В обработке», а текст сообщения оставался для «Подумать» при текущем статусе «Подумать»


## [2026-01-06] - Отклики: обновление списка после смены статуса
### Добавлено
- Унифицированная функция повторной загрузки откликов после действий в модалке смены статуса

### Изменено
- Логика загрузки откликов вынесена в отдельный метод для повторного использования без дублирования кода

### Исправлено
- Ошибка TypeScript «Cannot find name 'refreshApplications'» в `VacancyApplicationsPageClient.tsx`


## [2026-01-05] - Чат: безопасный HTML в сообщениях
### Добавлено
- Очистка и рендер HTML-разметки входящих сообщений в списке комнат и ленте переписки через DOMPurify

### Изменено
- Сообщение-превью в списке комнат отображается через безопасный HTML вместо plain text

### Исправлено
- Предотвращено некорректное отображение преформатированного HTML из бекэнда в чатах

## [2026-01-05] - Отклики: ограничения переходов статусов
### Добавлено
- Жёсткие правила переходов статусов отклика в модалке смены статуса, соответствующие последовательности процесса найма

### Изменено
- Для статуса «В обработке» скрыты чекбокс «Отправить сообщение» и поле ввода сообщения в модалке

### Исправлено
- Модалка не открывается для откликов со статусом «Отказ»; исключены недопустимые переходы статусов

## [2026-01-05] - Отклики: разделённые поля ФИО
### Добавлено
- Формирование ФИО из полей `first_name`, `last_name`, `middle_name` в списке откликов и модалке смены статуса

### Изменено
- Ответы бекэнда по откликам больше не используют `full_name`; фронтенд адаптирован к новым полям
- Шаблоны сообщений в модалке используют только `first_name` (без fallback на фамилию)

### Исправлено
- Некорректные обращения к отсутствующему полю `full_name` в UI откликов

## [2026-01-05] - Отклики: кнопка «Просмотрено» без модалки
### Добавлено
- Обработка кнопки «Просмотрено» через прямой PUT `/api/applications/{application_id}/employer-status` в статус `in_progress`

### Изменено
- Карточка отклика сразу удаляется из списка «Неразобранные» после успешного обновления статуса, модалка не открывается

### Исправлено
- Исключено открытие модалки смены статуса при клике «Просмотрено»
- Параметр запроса обновления статуса приведён к схеме бэкенда (`status` вместо `employer_status`) для корректной обработки 400

## [2025-12-31] - Предложение вакансии по резюме
### Добавлено
- Кнопка «Предложить вакансию» на публичной странице резюме `/resume/[link_uuid]` для работодателя
- Модалка выбора вакансии работодателя (список по `/api/companies/jobs`) с подтверждением отправки предложения по выбранной вакансии и `resume_link_uuid`
- Возможность отправить предложение из списка резюме на странице `/resume` через карточку резюме

### Изменено
- При успешной отправке предложения по вакансии отображается уведомление «Предложение по вакансии отправлено. Соискателю пришло письмо и сообщение в чат.» и, при наличии `chat_room_id` в ответе, кнопка «Перейти в чат» с переходом в `/chat/[chat_room_id]`
- В тело запроса `POST /api/jobs/{job_id}/offer` добавлено поле `message_text` (до 5000 символов), содержащее индивидуальное сообщение работодателя для первого сообщения в чат и письма соискателю
- Для повторных попыток отправки предложения по той же паре «вакансия + резюме» при ответе 400 с сообщением о дубликате кнопка «Предложить» блокируется (disabled)

### Исправлено
- Отображение и поведение кнопки «Предложить вакансию» завязаны на роль `employer`; для других ролей она не показывается, а ошибки 401/403 корректно обрабатываются на фронте (уведомление и редирект на логин)
- Ошибка 400 с текстом «Длина сообщения не должна превышать 5000 символов» подсвечивается под полем ввода текста предложения, без общего системного сообщения


## [2026-01-02] - Обновление контактов резюме
### Добавлено
- Заменены флаги has_whatsapp/has_telegram на строковые поля whatsapp/telegram в формах создания и редактирования резюме.
- Добавлено отображение строковых контактов WhatsApp/Telegram в списке откликов работодателя.
### Изменено
- Обновлена логика handlePartialSave для передачи строковых контактов и очистки значений через null.
- Обновлён UI модалки контактов: текстовые поля для WhatsApp и Telegram вместо чекбоксов.
- Обновлён вывод контактов резюме (страница откликов работодателя) под строковые поля.
### Исправлено
- Устранены ошибки отправки boolean флагов для WhatsApp/Telegram при multipart запросах.


## [2026-01-02] - Контакты резюме: WhatsApp/Telegram как строки
### Добавлено
- Поддержка строковых полей `whatsapp` и `telegram` в запросах и ответах фронтенда вместо булевых флагов наличия контактов в резюме

### Изменено
- В запросах создания и обновления резюме фронтенд перестал отправлять флаги `has_whatsapp`/`has_telegram` и перешел на использование полей `whatsapp`/`telegram` (строки или `null`)
- Страница просмотра откликов работодателя `/employer/vacancies/[id]/applications` теперь использует строковые поля `whatsapp`/`telegram` из блока резюме в ответе API вместо булевых флагов

### Исправлено
- Исключены обращения к устаревшим полям `has_whatsapp`/`has_telegram` во фронтенде, что предотвращает рассинхронизацию с обновленным API


## [2026-01-02] - Визуальное отображение комментария к телефону
### Добавлено
- Нет
### Изменено
- Комментарий к телефону в блоке контактов резюме теперь отображается в скобках сразу после номера.
### Исправлено
- Нет

## [2026-01-03] - Публичное резюме: контакты и ФИО из contacts
### Добавлено
- Типы `ResumeContacts`, `ResumeByLinkItem`, `SearchResumesByLinkResponse` для ответа `GET /api/resumes/link/{link_uuid}`
### Изменено
- Публичная страница резюме `/resume/[link_uuid]` теперь берёт ФИО из блока `contacts` и использует новый ответ API (`resume` + `contacts`)
### Исправлено
- Нет

## [2026-01-03] - Публичное резюме: отображение блока контактов
### Добавлено
- Блок «Контакты» в шапке `/resume/[link_uuid]` с иконками и выводом телефона (с комментарием), email, сайта, WhatsApp, Telegram из `contacts`
### Изменено
- Нет
### Исправлено
- Нет

## [2026-01-03] - Публичное резюме: корректный показ кнопки загрузки контактов
### Добавлено
- Нет
### Изменено
- Кнопка «Показать контакты» теперь отображается только когда `contacts` в ответе `GET /api/resumes/link/{link_uuid}` равен `null`; при наличии объекта без значений выводится текст «Контакты не указаны»
### Исправлено
- Нет

## [2026-01-03] - Публичное резюме: обновление ФИО после загрузки контактов
### Добавлено
- Нет
### Изменено
- Заголовок с ФИО в шапке `/resume/[link_uuid]` пересчитывается после получения контактов через `POST /api/resumes/link/{link_uuid}/contacts`, включая поля `full_name` из ответа; fallback на title убран
### Исправлено
- Нет

## [2026-01-03] - Публичное резюме: обновление фото из POST контактов
### Добавлено
- Нет
### Изменено
- После запроса `POST /api/resumes/link/{link_uuid}/contacts` фото (`photo_url`) из ответа применяется в шапке публичного резюме
### Исправлено
- Нет

## [2026-01-03] - Резюме: фото только через contacts
### Добавлено
- Нет
### Изменено
- Поле `photo_url` убрано из типа `Resume`; карточка резюме (ResumeCard) показывает только заглушку, фото берётся из контактов на публичной странице
- Увеличен размер аватара в шапке публичного резюме и прижат к верхнему краю блока
### Исправлено
- Нет

## [2026-01-04] - Модалка смены статуса отклика
### Добавлено
- Черновик описания модального окна смены статуса отклика с полем сообщения, чекбоксом «Отправить сообщение», подстановкой названия компании и ссылкой на резюме
### Изменено
- Запланировано открывать модалку при всех кликах смены статуса вместо выпадающего меню
### Исправлено
- Нет

## [2026-01-03] - Плавающая кнопка «Предложить вакансию» на публичном резюме
### Добавлено
- Фиксированный блок с кнопкой «Предложить вакансию», появляющийся при скрытии основной кнопки из области видимости (IntersectionObserver)
### Изменено
- Плавающий блок растягивается на ширину контейнера страницы (max-w-5xl) и центрируется по горизонтали, не выходя за пределы контента
- У блока убрано скругление верхней границы и задан меньший отступ снизу для корректного прилипания к контейнеру
- Кнопки действий (избранное, скачать, печать) перенесены в панель с основной и плавающей кнопкой «Предложить вакансию» и прижаты вправо
- В шапке резюме блок с кнопкой «Предложить вакансию» и действиями перенесён под основную информацию и контакты, выравнен вправо
- Основные секции резюме (Опыт, Навыки, Языки, Обо мне, Образование) выровнены в две колонки: заголовок слева, контент справа
- Панель «Предложить вакансию» встроена в нижнюю часть основного контента резюме; уменьшён нижний отступ страницы (pb-12)
- Уменьшен внутренний отступ панели «Предложить вакансию» снизу (py-3) для компактности
### Исправлено
- —

## [2026-01-02] - Резюме: возраст и контакты в карточке
### Добавлено
- Нет
### Изменено
- В карточке резюме (ResumeCard) при отсутствии возраста выводится текст «Возраст не указан», при этом пол показывается всегда.
- В блоке контактов резюме Email и Веб-сайт скрываются, если значения отсутствуют.
- На публичной странице резюме возраст показывает «Возраст не указан», если поле отсутствует, при этом пол выводится всегда.
### Исправлено
- Нет


## [2026-01-01] - Обновление ответов резюме (search/link)
### Добавлено
- Поддержка нового формата ответов `/api/resumes/search` и `/api/resumes/link/{link_uuid}` на фронтенде

### Изменено
- Из структуры ответа резюме удалены контактные поля (`phone`, `email`, `phone_comment`, `website_url`, `has_whatsapp`, `has_telegram`) для публичного показа резюме
- Вместо поля `birth_date` в `job_seeker_profile` используется `age_years` (готовый возраст в годах), фронтенд больше не рассчитывает возраст по дате рождения
- Для загруженных фото в карточке резюме добавлена рамка вокруг аватарки кандидата

### Исправлено
- Компоненты карточки резюме и публичной страницы резюме адаптированы к новому формату: убраны обращения к удалённым полям и переработан вывод возраста
- Публичная страница резюме корректно работает с правилами скрытия ФИО: если ФИО отсутствует в ответе, в заголовке показывается `title` резюме или слово «Кандидат»


## [2025-12-31] - Поиск резюме: быстрые фильтры
### Добавлено
- Быстрые фильтры на странице поиска резюме `/resume` (локация, статус поиска, тип занятости, график работы, общий опыт, уровень дохода), связанные с теми же полями, что и в `ResumeFiltersModal`

### Изменено
- Кнопки быстрых фильтров теперь открывают отдельные модальные окна по каждой группе параметров и подсвечиваются при наличии соответствующих активных параметров в текущем наборе фильтров
- Настройки, сделанные в отдельных модалках быстрых фильтров, синхронизируются с общей модалкой фильтров резюме и наоборот

### Исправлено
- —


## [2025-12-30] - Управление вакансиями работодателя: просмотр откликов
### Добавлено
- Ссылка на публичное резюме соискателя из списка откликов работодателя на странице `/employer/vacancies/[id]/applications` с открытием в новой вкладке

### Изменено
- Заголовок с названием резюме (`resume_title`) в карточке отклика теперь является ссылкой на страницу `/resume/[link_uuid]` и дополнен иконкой стрелки вверх-вправо для явного указания перехода
- Цвет текста в блоке основной информации по отклику (название резюме, ФИО, опыт, контакты) унифицирован до чёрного для повышения читаемости
- Обновлена иконка ссылки на резюме: убраны вспомогательные линии, оставлена только диагональная стрелка вверх-вправо в минималистичном стиле

### Исправлено
- —



## [2025-12-30] - Фильтры поиска резюме: локации
### Добавлено
- Поддержка выбора городов и регионов в модальном окне фильтров поиска резюме через единый эндпоинт `/api/locations`

### Изменено
- Логика парсинга ответа `/api/locations` в `ResumeFiltersModal` переведена на использование полей `id` и `type` (`city`/`reg`) с маппингом в `city_id`/`region_id`

### Исправлено
- Некорректное формирование списка подсказок локаций при поиске по строке (результаты не добавлялись в `selectedLocations` и не отображались в UI)

## [2026-02-19] - Отображение названия вакансии в откликах
### Добавлено
- Поле `job_title` в тип `Application` для хранения названия вакансии
- Отображение названия вакансии в заголовке страницы для контекста

### Изменено
- Заголовок страницы показывает название вакансии после "Отклики по вакансии:"

### Исправлено
- Нет


## [2026-01-06] - Чат: блокировка отправки при отказе
### Добавлено
- Блокировка поля ввода и кнопки отправки сообщений при статусе работодателя `rejected` в ответе чата

### Изменено
- Использование поля `employer_status` из ответа `/api/chat/messages/{id}` для управления доступностью отправки сообщений

### Исправлено
- Предотвращена отправка новых сообщений после смены статуса работодателя на `rejected`

## [2026-02-21] - Модалка откликов: скрытие ФИО при массовом отказе
### Добавлено
- Нет

### Изменено
- В модалке смены статуса при массовом действии ("Отказать всем") скрыты ФИО и ссылка на резюме первого соискателя
- При массовом действии в модальном окне доступен только один вариант статуса — "Отказ"

### Исправлено
- Логическое отображение элементов (ФИО и выбор других статусов) при массовом отказе

## [2026-02-28] - Массовый отказ: текст сообщения без имени
### Добавлено
- Нет

### Изменено
- Модалка предложения вакансии: при наличии одной вакансии она выбирается автоматически

### Исправлено
- При действии "Отказать всем" в модалке сообщения больше не подставляется имя соискателя
- Для откликов в статусе "Отказ" (rejected) кнопка "✗ Отказано" больше не открывает модалку смены статуса


## [2026-03-05] - Публичное резюме: скрытые контакты и ФИО
### Добавлено
- Нет

### Изменено
- Публичная страница резюме отображает текст «Контакты скрыты соискателем» вместо кнопки «Показать контакты», если `contacts` в ответе `POST /api/resumes/link/{link_uuid}/contacts` равен `null`
- Публичная страница резюме отображает текст «ФИО скрыто соискателем» в заголовке вместо «Кандидат», если `full_name` в ответе равен `null`

### Исправлено
- Нет


## [2026-04-23] - Блок "Похожие вакансии" на странице детальной вакансии
### Добавлено
- SSR-загрузка и отображение похожих вакансий на странице `/vacancy/[id]` через эндпоинт `GET /api/jobs/{job_id}/similar?n=6`
- Два варианта заголовка блока: «Похожие вакансии» (variant=profession) и «Другие вакансии компании» (variant=company)
- Карточки похожих вакансий с названием, зарплатой, опытом, форматом работы, адресом и логотипом компании
- Интерфейсы `SimilarJobItem`, `SimilarJobsResponse` для типизации ответа API

### Изменено
- Страница `/vacancy/[id]/page.tsx` расширена для SSR-загрузки и отображения блока похожих вакансий

### Исправлено
- Нет

## [2026-04-22] - Блок "Работа в других регионах" на странице вакансий по городу
### Добавлено
- Блок со ссылками на соседние города региона в нижней части страницы `/vakansii/[slug]` над футером
- Получение ID города через эндпоинт `/api/dictionaries/cities/by-slug/{slug}`
- Загрузка соседних городов через эндпоинт `/api/dictionaries/cities/{id}/neighbors` с параметрами `population=20000` и `limit=10`
- Отображение городов в виде скругленных кнопок-ссылок вида `/vakansii/{slug}`

### Изменено
- Страница `/vakansii/[slug]/page.tsx` расширена для отображения блока с городами региона

### Исправлено
- Нет

## [2026-03-06] - Публичное резюме: флаг открытия контактов
### Добавлено
- Нет

### Изменено
- Публичная страница резюме `/resume/[link_uuid]` адаптирована под флаг `contacts_opened` в ответе `GET /api/resumes/link/{link_uuid}`
- Обновлена логика отображения CTA «Открыть контакты» и текста «Контакты скрыты соискателем» в зависимости от `contacts_opened` и наличия доступных полей в `contacts`

### Исправлено
- Нет
