# План реализации фронтэнда платформы поиска работы JobFind

## Задача: Вынести создание и редактирование вакансий работодателя на отдельные страницы
- **Статус**: В процессе
- **Описание**: Перенести создание вакансии на `/employer/vacancies/add`, редактирование — на `/employer/vacancies/[id]/edit`, а страницу `/employer/vacancies` оставить как список вакансий с навигацией. При создании/редактировании не реализовывать поля районов и станций метро у адресов вакансии и не отправлять их в запросах (как в текущей реализации).
- **Шаги выполнения**:
  - [ ] Актуализировать документацию (changelog, plan)
  - [ ] Проанализировать текущую реализацию `/employer/vacancies` и сбор payload для API
  - [ ] Вынести форму вакансии в переиспользуемый компонент (общий для add/edit)
  - [ ] Реализовать страницу создания `app/employer/vacancies/add/page.tsx` (POST `/api/jobs`)
  - [ ] Реализовать страницу редактирования `app/employer/vacancies/[id]/edit/page.tsx` (PUT `/api/jobs/{job_id}`; отправлять полное тело)
  - [ ] Обновить `/employer/vacancies`: оставить список + ссылки на add/edit
  - [ ] Провести self-review и визуальную проверку
- **Зависимости**: `POST /api/jobs`, `PUT /api/jobs/{job_id}`, роль `employer`, профиль компании работодателя

## Задача: Унификация форм профиля компании работодателя (/employer/addcompany и /employer/mycompany)
- **Статус**: В процессе
- **Описание**: Привести формы создания и редактирования профиля компании к единому UX и одинаковому порядку полей. Унифицировать группировку полей в строки (ИНН+тип, телефон+email, сотрудники+год основания), а также поведение выбора отраслей (кнопка «Выбрать», закрытие по клику вне без применения).
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [x] Сравнить текущие реализации форм и привести структуру полей/валидации к единому виду
  - [x] Унифицировать порядок и лейаут полей на обеих страницах
  - [x] Унифицировать компонент/логику выбора отраслей (кнопка «Выбрать», закрытие по клику вне)
  - [ ] Сделать обязательными поля: официальное название, ИНН, тип, отрасли
  - [ ] Провести self-review и визуальную проверку
- **Зависимости**: `GET/POST /api/companies/profile`, справочники отраслей

## Задача: Интерфейс чата (страница /chat и модальное окно)
- **Статус**: В процессе
- **Описание**: Реализовать единый интерфейс чатов для соискателей и работодателей: отдельную страницу `/chat` и модальное окно, открывающееся из плавающей кнопки на всех страницах (кроме самой страницы `/chat`). Интегрировать REST- и WebSocket-API чата (комнаты, сообщения, статусы прочтения, индикатор набора текста), добавить пункт "Чаты" в верхнее меню. Отобразить статусы прочтения сообщений для отправителя (одинарная/двойная галка).
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan, architecture) по модулю чата
  - [ ] Добавить конфигурацию REST-эндпоинтов чата в `app/config/api.ts` и вспомогательные функции в `app/utils/api.ts`
  - [ ] Реализовать страницу `app/chat/page.tsx` с комнатами, фильтром "Только непрочитанные" и панелью сообщений
  - [ ] Реализовать плавающую кнопку и модальное окно чата, переиспользуя логику страницы `/chat` (кнопка скрыта на самой странице `/chat`)
  - [ ] Вынести WebSocket base URL в переменную окружения `NEXT_PUBLIC_WS_BASE_URL` (dev fallback `localhost:8081`, prod `wss://e77.top`)
  - [x] Интегрировать WebSocket-клиент: подключение по `ws(s)://localhost:8081/ws/chat/{room_id}` в dev-окружении (Go-бэкенд на порту 8081), обработка событий `message`, `read`, `typing`, `room_list_refresh`
  - [x] Реализовать индикацию статуса прочтения исходящих сообщений (одинарная/двойная галка в списке сообщений)
  - [ ] Добавить пункт/иконку "Чаты" в верхнее меню для ролей `job_seeker` и `employer` (переход на страницу `/chat`)
  - [ ] Провести визуальную проверку, self-review и обновить `docs/architecture.md`
- **Зависимости**: REST/WS API чата бекэнда (`/api/chat/*`, `/ws/chat/{room_id}`)


## Задача: Страница просмотра резюме по ссылке /resume/[link_uuid]
- **Статус**: В процессе
- **Описание**: Реализовать страницу `/resume/[link_uuid]` для просмотра резюме по публичной ссылке `link_uuid`, используя данные эндпоинта `GET /api/resumes/link/{link_uuid}`. Страница должна отображать полную карточку кандидата с блоками: общая информация, опыт работы, навыки, языки, образование, контакты.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (swagger, changelog, plan, architecture)
  - [ ] Добавить эндпоинт в `API_ENDPOINTS.resumes` и, при необходимости, вспомогательную функцию загрузки
  - [x] Создать страницу `app/resume/[link_uuid]/page.tsx` с детальной версткой резюме (шапка с ФИО, блок резюме, панель кнопок справа)
  - [ ] Реализовать обработку состояний загрузки, ошибки и отсутствия резюме
  - [ ] Провести визуальную проверку и self-review
- **Зависимости**: `GET /api/resumes/link/{link_uuid}`, типы `Resume` и `SearchResumesResponse`

## Задача: Страница поиска резюме для работодателей /resume
- **Статус**: Завершена
- **Описание**: Создать страницу `/resume` для поиска резюме, доступную только работодателям. Реализовать интерфейс поиска с фильтрами, списком резюме и пагинацией. Интеграция с `/api/resumes/search`.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [x] Создать типы для резюме в `app/types/resume.ts`
  - [x] Добавить эндпоинт `search` в `app/config/api.ts`
  - [x] Обновить хук `useUser` для поддержки состояния загрузки (`isLoading`)
  - [x] Создать компонент карточки резюме `app/components/ResumeCard.tsx`
  - [x] Создать страницу `app/resume/page.tsx` с логикой поиска и проверкой роли
  - [x] Создать модальное окно фильтров `app/components/ResumeFiltersModal.tsx`
  - [x] Интегрировать фильтры: регион, статус поиска, возраст, фото, тип занятости, график работы, опыт, специализация, пол, зарплата, образование, навыки, права, языки
  - [x] Реализовать поиск профессий через `/api/dictionaries/professions/search`
  - [x] Реализовать поиск локаций через `/api/locations`
- **Зависимости**: `/api/resumes/search` (POST), `/api/dictionaries/professions/search` (GET), `/api/locations` (GET), роль `employer`

## Задача: Модальное окно отклика на вакансию
- **Статус**: Завершена
- **Описание**: Реализовать модальное окно для отправки откликов на вакансии для зарегистрированных пользователей с ролью `job_seeker`. Модальное окно должно содержать выбор резюме, поле для сопроводительного письма и предупреждение о возможном отказе.
- **Шаги выполнения**:
  - [x] Создать компонент `ApplicationModal` с функционалом выбора резюме и отправки отклика
  - [x] Добавить endpoints для откликов в `app/config/api.ts`
  - [x] Создать клиентские компоненты для кнопок отклика на страницах `/vacancy/[id]` и `/vakansii/[slug]`
  - [x] Интегрировать модальное окно в страницу списка вакансий `/vacancy`
  - [x] Реализовать редирект на страницу входа для неавторизованных пользователей
  - [x] Сохранить SSR на страницах детальной вакансии
  - [x] Обновить документацию (changelog, plan)
- **Зависимости**: API endpoint `POST /api/applications`, `GET /api/resumes/my`, хук `useUser`, компонент `ApplicationModal`

## Задача: Страница подтверждения email
- **Статус**: Завершена
- **Описание**: Создать страницу `/login/verify-email` для подтверждения email пользователя по токену из письма, отправляемого при регистрации.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan, architecture)
  - [x] Добавить тип `VerifyEmailRequest` в `app/types/auth.ts`
  - [x] Добавить эндпоинт `verifyEmail` в `app/config/api.ts`
  - [x] Добавить метод `verifyEmail` в `authApi` (`app/utils/api.ts`)
  - [x] Создать страницу `app/login/verify-email/page.tsx` с обработкой токена из query-параметров
  - [x] Реализовать три состояния: загрузка, успех (с автоматическим редиректом на `/login`), ошибка
  - [x] Добавить визуальную индикацию статуса подтверждения
- **Зависимости**: Эндпоинт `/api/auth/verify-email` (POST) из swagger

## Задача: Обновление регистрации/логина с подтверждением email и переотправкой письма
- **Статус**: В процессе
- **Описание**: Привести страницы регистрации (соискатель/работодатель) и логина к новой логике бэкенда: регистрация возвращает только сообщение, логин для неверного email/пароля возвращает `401`, а для неподтверждённого email возвращает `403`. Реализовать экран «Подтвердите email» с переотправкой письма на тот же email и сценарием смены email через `POST /api/auth/resend-verification`, добавить cooldown 120 секунд и корректную обработку ошибок `429/400/404`.
- **Шаги выполнения**:
  - [x] Зафиксировать изменения в `docs/changelog.md` и добавить задачу в `docs/plan.md`
  - [x] Обновить `app/config/api.ts`, `app/types/auth.ts`, `app/utils/api.ts` для поддержки `resend-verification` и новой структуры ответов регистрации
  - [x] Обновить страницы регистрации: `/register` и `/register/employer` (редирект на экран подтверждения, передача email)
  - [x] Обновить страницу `/register/verify-email`: переотправка письма, смена email, cooldown 120 секунд, обработка `429/400/404`
  - [x] Обновить страницу `/login`: при неуспешном логине (401) показывать экран подтверждения email с переотправкой/сменой email
  - [ ] Провести self-review изменений и визуальную проверку UX
- **Зависимости**: `/api/auth/register`, `/api/auth/login`, `/api/auth/resend-verification`, `/api/auth/verify-email`

## Задача: Восстановление пароля (anti-enumeration UX)
- **Статус**: Завершена
- **Описание**: Реализовать поток восстановления пароля без раскрытия существования email: на шаге запроса восстановления `POST /api/auth/forgot-password` всегда показывать нейтральный success-текст при `200`. Добавить страницу `/reset-password` (token из query) и обработку ответов `200/400/404` для `POST /api/auth/reset-password`.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [x] Обновить страницу `/forgot-password`: нейтральный success на `200`, обработка `400/500` без раскрытия существования email
  - [x] Добавить страницу `/reset-password`: чтение `token` из query, отправка `POST /api/auth/reset-password`, обработка `200/400/404`, кнопка «Запросить новую ссылку»
  - [x] Провести self-review изменений и визуальную проверку UX
- **Зависимости**: `/api/auth/forgot-password`, `/api/auth/reset-password`, письмо со ссылкой `FRONTEND_URL/reset-password?token=...`

## Задача: Типографика описания вакансии (Tailwind Typography)
- **Статус**: В процессе
- **Описание**: Подключить плагин `@tailwindcss/typography` и применить его к HTML-описанию вакансий на странице `app/vacancy/[id]/page.tsx`, чтобы улучшить читаемость и единообразие типографики.
- **Шаги выполнения**:
  - [x] Зафиксировать изменения в `docs/changelog.md` и `docs/plan.md`
  - [ ] Установить пакет `@tailwindcss/typography` и подключить плагин в `tailwind.config.js`
  - [ ] Проверить рендеринг описания вакансии (`job.description`) и при необходимости скорректировать набор классов `prose`
  - [ ] Провести визуальную проверку и self-review
- **Зависимости**: Tailwind CSS, страница детальной вакансии `/vacancy/[id]`

## Задача: SEO-метатеги детальной вакансии
- **Статус**: В процессе
- **Описание**: Реализовать шаблон формирования Title и Description в `app/vacancy/[id]/page.tsx`, который использует данные вакансии (название, город, зарплата, компания, даты публикации/обновления, опыт работы) для улучшения SEO и соответствия требованиям поиска.
- **Шаги выполнения**:
  - [x] Зафиксировать требования в changelog и плане
  - [ ] Обновить `docs/architecture.md` описанием источников данных и правил шаблона
  - [ ] Внедрить новый шаблон `generateMetadata`
  - [x] Добавить в документацию ссылку на использование `city_name_prepositional` и скорректировать SEO-описание
  - [ ] Провести проверку и self-review
- **Зависимости**: `GET /api/jobs/{job_id}` (JobPosting), поля `salary_min`, `salary_max`, `company_profile.company_name`, `cities`, `address`, `posted_date`, `updated_at`, `work_experience`

## Задача: Текстовый блок опыта и формата работы (14px)
- **Статус**: В процессе
- **Описание**: Унифицировать типографику текстового блока «Опыт работы / Формат работы» на странице `app/vacancy/[id]/page.tsx`, повысив базовый размер шрифта до фиксированных 14px через Tailwind-класс `text-[14px]` для лучшей читабельности.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan, architecture) по предстоящей правке UI
  - [ ] Обновить Tailwind-классы блока, чтобы текст рендерился с адаптивными размерами `text-sm md:text-base` и без `font-medium` у лейблов
  - [ ] Добавить текстовые строки «Тип графика работы», «График работы», «Часы работы», «Смены», «Занятость», «Образование» (на основе соответствующих справочников) при наличии данных
  - [ ] Обработать `salary_frequency`: поддержка значений из RU/EN enum и вывод строки «Выплаты: ...» внутри того же блока, что и зарплата
  - [ ] Провести визуальную проверку и self-review изменений
- **Зависимости**: Страница детальной вакансии `/vacancy/[id]`

## Задача: Промо-редирект по внешней ссылке вакансии
- **Статус**: В процессе
- **Описание**: Для промо-вакансий (`is_promo = true`) кнопка «Откликнуться» должна открывать промежуточную страницу `/vacancy/[id]/to`, которая выполняет серверный редирект на `link` вакансии. Для остальных вакансий поведение остаётся прежним.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan, architecture)
  - [ ] Создать страницу `app/vacancy/[id]/to/page.tsx` с серверной загрузкой вакансии и редиректом на `link`
  - [ ] Обновить кнопки «Откликнуться» на `app/vacancy/[id]/page.tsx` и списке `/vakansii/[slug]`, чтобы промо-вакансии открывали новую вкладку `/vacancy/[id]/to` независимо от наличия `link` в выдаче
  - [ ] Провести проверку и self-review
- **Зависимости**: `API_ENDPOINTS.jobById`, корректные значения `is_promo` и `link`

## Задача: Перенос городских страниц на `/vakansii/[slug]`
- **Статус**: В процессе
- **Описание**: Изменить маршрут списка вакансий по городу на `/vakansii/[slug]` для устранения конфликта с `/vacancy/[id]`. Обновить ссылки на главной странице.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [ ] Создать страницу `app/vakansii/[slug]/page.tsx` (SSR)
  - [ ] Обновить ссылки на главной с `/vacancy/{slug}` на `/vakansii/{slug}`
  - [ ] (Опционально) Настроить редирект/удалить `app/vacancy/[slug]`
- **Зависимости**: `/api/jobs/searchBySlug` 

## Задача: Страница списка по городу `/vakansii/[slug]`
- **Статус**: В процессе
- **Описание**: Создать SSR-страницу со списком вакансий по городу. Данные загружать через `GET /api/jobs/searchBySlug?city_slug={slug}`. Оформление и карточки совпадают со страницей `/vacancy`. SEO-метаданные формируются на основе `name_prepositional` из `/api/dictionaries/cities/by-slug/{slug}`.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [x] Реализовать страницу `app/vakansii/[slug]/page.tsx` (SSR) с серверной загрузкой вакансий
  - [x] Добавить SEO-метаданные `generateMetadata` с запросом в `/api/dictionaries/cities/by-slug/{slug}` и использованием `name_prepositional`
  - [x] Добавить текстовый индикатор диапазона «Показаны X–Y из N вакансий» и плавный скролл наверх при смене страницы (через общий компонент пагинации)
  - [ ] Визуальная проверка и краткий self-review
- **Зависимости**: `docs/swagger.yaml` — эндпоинты `/api/jobs/searchBySlug` и `/api/dictionaries/cities/by-slug/{slug}`

## Задача: Блок «Работа в других городах» на главной
- **Статус**: В процессе
- **Описание**: Добавить внизу главной страницы секцию со списком 10 крупных городов РФ. Каждая ссылка ведёт на `/vacancy/{slug}`. Список городов задаётся вручную на фронтенде.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [ ] Реализовать секцию на `app/page.tsx` с адаптивной сеткой и hover-ссылками
  - [ ] Задать ручной список из 10 городов и корректные slug
  - [ ] Визуальная проверка и краткий self-review
- **Зависимости**: Отсутствуют (статический список городов)

## Задача: Фикс загрузки логотипа в форме компании
- **Статус**: Завершена
- **Описание**: Устранить ошибку типизации в `app/employer/addcompany/page.tsx`, возникающую при обновлении `logo` после загрузки файла. Гарантировать, что состояние содержит все обязательные поля и соответствует API.
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan, architecture)
  - [x] Исправить типы и логику обновления `formData.logo`
  - [x] Проверить сборку `npm run build`
- **Зависимости**: `uploadFile` из `app/utils/api.ts`, контракты `CompanyProfile`

## Задача: Пагинация списка вакансий на /vacancy
- **Статус**: В процессе
- **Описание**: Добавить пагинацию на клиентскую страницу `/vacancy` с использованием общего компонента `Pagination`, чтобы вакансии подгружались постранично по 50 штук, а фильтры и сортировка сохранялись в query-параметрах.
- **Шаги выполнения**:
  - [x] Зафиксировать изменения в changelog, плане и архитектуре
  - [ ] Обновить клиентский запрос `/api/jobs` для работы с `page` и `limit=50`, а также с пагинированным ответом
  - [x] Интегрировать компонент `Pagination` на странице `/vacancy` и вывести общее количество вакансий
  - [ ] Улучшить UX пагинации: скролл к началу списка при смене страницы и отображение диапазона "Показаны X–Y из N"
  - [ ] Провести визуальную проверку и self-review пагинации
- **Зависимости**: Поведение эндпоинта `/api/jobs` (поддержка `page` и `limit`, структура пагинированного ответа, аналогичная `/api/jobs/searchBySlug`).

## Задача: Устранение ошибок сборки (useSearchParams и конфигурация Next.js)
- **Статус**: В процессе
- **Описание**: Исправить падение `next build`, вызванное использованием `useSearchParams()` вне `<Suspense>` на страницах `/profile`, `/employer/mycompany`, `/vacancy`, а также удалить невалидный ключ `experimental.telemetry` из `next.config.js`.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan, architecture) с описанием необходимых изменений
  - [ ] Обновить `next.config.js`, убрав некорректные ключи и зафиксировав стратегию проксирования API
  - [ ] Перевести `/profile`, `/employer/mycompany`, `/vacancy` на CSR через `next/dynamic` с `{ ssr: false }` и клиентские компоненты `*PageClient`
  - [ ] Проверить вложенные компоненты (`JobFilters` и др.) на использование `useSearchParams`; для клиентских страниц это допустимо, SSR отключён
  - [ ] Прогнать `npm run build` и убедиться в отсутствии ошибок
- **Зависимости**: Next.js App Router, компоненты `app/profile/page.tsx`, `app/employer/mycompany/page.tsx`, `app/vacancy/page.tsx`, `app/components/JobFilters.tsx`

## Задача: Редактирование резюме — обработчик поля «Желаемая должность»
- **Статус**: Завершена
- **Описание**: Добавить отсутствующий обработчик `handleTitleChange` в `app/resume/[id]/edit/page.tsx`, чтобы автодополнение профессий работало корректно и сборка не падала по ошибке типов.
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan, architecture)
  - [x] Реализовать `handleTitleChange` с вызовом поиска профессий
  - [x] Протестировать ввод и заново запустить `npm run build`
- **Зависимости**: API автодополнения профессий, локальное состояние формы резюме

## Задача: Детальная страница вакансии /vacancy/[id] (SSR)
- **Статус**: В процессе
- **Описание**: Реализовать серверный компонент страницы детального просмотра вакансии с загрузкой `GET /api/jobs/{job_id}`, хлебными крошками, блоками как на макете и кнопкой «Откликнуться»-заглушкой. Обрабатывать состояния загрузки/ошибок/404.
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [ ] Создать `app/vacancy/[id]/page.tsx` (server component, SSR)
  - [ ] Добавить хлебные крошки вверху страницы
  - [ ] Сверстать разделы: заголовок/зарплата/лейблы/адрес, сведения о компании, условия, обязанности (description), требования (если появятся в API), нижняя панель действий
  - [ ] Обработать ошибки и 404 через `notFound()`
  - [ ] Визуальная проверка адаптивности
  - [ ] [A] Визуальные правки под макет (отступы, размеры шрифтов, оформление «Условия»/«Требования»)
  - [ ] [B] SEO: `generateMetadata` с заголовком на основе названия и города
  - [x] [C] Прелоадер: `app/vacancy/[id]/loading.tsx` (skeleton)
  - [ ] [D] SSR fetch использует абсолютный URL, собранный из заголовков (`headers()`), и аналогично в `generateMetadata`
- **Зависимости**: `docs/swagger.yaml` (models.JobPosting), корректное проксирование `/api` на 8080

## Задача: Структурированные данные JobPosting на /vacancy/[id]
- **Статус**: В процессе
- **Описание**: Добавить на страницу `app/vacancy/[id]/page.tsx` JSON-LD разметку `schema.org/JobPosting`, чтобы вакансии корректно индексировались Google и попадали в расширенные результаты.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan, architecture)
  - [ ] Сформировать объект JobPosting на основе данных вакансии (зарплата, адрес, работодатель, описание, идентификатор) и встроить через `<script type="application/ld+json">`
  - [ ] Отразить удалённые вакансии: если `work_formats` содержит значение с ID `1`, проставить `jobLocationType: "TELECOMMUTE"` и текст о работе из дома; иначе сформировать `jobLocation` с адресом и городом (country code всегда `RU`)
  - [ ] Удалить из JSON-LD поля `identifier`, `hiringOrganization.logo`, `hiringOrganization.sameAs`, заполнять `datePosted` как «текущая дата минус 1 день», а `employmentType` маппить на значения `FULL_TIME`, `PART_TIME`, `CONTRACTOR`, `INTERN`, `VOLUNTEER` по справочнику `app/config/employment_types_202505222228.json`
  - [x] Привести интерфейсы и использование `job` на `app/vacancy/[id]/page.tsx` к новой схеме `controllers.JobDetailResponse` из swagger (`GET /api/jobs/{job_id}`), включая устранение дублирующихся полей в `JobPosting`.
  - [ ] Обработать сетевые ошибки загрузки вакансии: при недоступности API выводить дружелюбное состояние (не бросать необработанное исключение)
  - [ ] Проверить результат в Rich Results Test и провести self-review
- **Зависимости**: `GET /api/jobs/{job_id}`, локальные справочники форматов работы

## Задача: HTML-рендер описания вакансии
- **Статус**: В процессе
- **Описание**: Обеспечить корректный вывод HTML-разметки, приходящей в `JobPosting.description`, на странице детальной вакансии вместо разбиения текста по переводам строк.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan, architecture)
  - [ ] Обновить `app/vacancy/[id]/page.tsx` для безопасного рендера HTML
  - [ ] Провести визуальную проверку и self-review блока «Обязанности»
- **Зависимости**: Бэкенд гарантирует валидацию/sanitization HTML содержимого `description`

## Задача: Редизайн UI детальной страницы вакансии — объединение блоков и плавающая панель
- **Статус**: В процессе
- **Описание**: Объединить информацию о вакансии в один визуальный блок и сделать панель действий плавающей внизу экрана. Кнопку «В избранное» заменить иконкой сердечка, «В черный список» и «Пожаловаться» перенести в выпадающее меню.
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [ ] Объединить блоки (заголовок/зарплата/лейблы/адрес + компания + условия + описание) в один контейнер
  - [ ] Сделать панель действий фиксированной у нижнего края экрана, добавить отступ контенту
  - [ ] Заменить текст «В избранное» на иконку сердечка (aria-label)
  - [ ] Перенести «В черный список» и «Пожаловаться» в выпадающее меню
  - [ ] Визуальная проверка на мобильных и десктопе
- **Зависимости**: TailwindCSS, SSR-компонент страницы

## Задача: Обновление отображения списка вакансий по /api/jobs (Вариант B)
- **Статус**: В процессе
- **Описание**: Привести страницу `/vacancy` к схеме `controllers.JobListItem`: заголовок со ссылкой на карточку, зарплата (диапазон/от/до) с периодом и типом налогообложения, компания, чипы форматов работы по `work_format_ids`, опыт по `work_experience`, адрес/город по правилу Варианта B (приоритет `address.city` → первый из `publication_cities`, совпадающий с выбранными в фильтрах, → иначе первый из `publication_cities`), логотип и кнопка «Откликнуться». Адаптивная верстка.
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [ ] Обновить интерфейсы и рендер `app/vacancy/page.tsx` под новый формат
  - [ ] Реализовать маппинг городов из URL (city_id) в имена через `API_ENDPOINTS.dictionaries.citiesByIds`
  - [ ] Визуально проверить адаптивность карточек
- **Зависимости**: `docs/swagger.yaml` (controllers.JobListItem), `app/config/work_formats_202505222228.json`, наличие эндпоинта cities-by-ids

## Задача: Диагностика ошибки /api/jobs (500 и прокси на :3000)
- **Статус**: В процессе
- **Описание**: Выявить причины 500 на GET /api/jobs и неверного проксирования на localhost:3000 при бэкенде на localhost:8080. Подготовить и согласовать вариант исправления.
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [x] Проанализировать next.config.js и middleware на предмет переписываний /api
  - [x] Проверить вызовы в app/vacancy/page.tsx (относительный путь vs API_ENDPOINTS)
  - [x] Согласовать вариант фикса (rewrites -> :8080 / заменить fetch на API_ENDPOINTS / создать proxy-роут в Next)
  - [ ] Изменить конфигурацию: использовать `afterFiles` для прокси `/api/:path* -> http://localhost:8080/api/:path*`, чтобы локальные `app/api/*` обрабатывались первым делом (учёт `/api/upload`)
  - [ ] Реализовать выбранный вариант и протестировать
- **Зависимости**: Корректный доступ к бэкенду на http://localhost:8080, актуальный swagger


## Задача: Обязательность города при создании/редактировании вакансии (Вариант A)
- **Статус**: Завершена
- **Описание**: Привести валидацию UI к Варианту A: swagger не изменяется (обязательные `title`, `description`, `work_experience`), на уровне UI город публикации обязателен (требуется хотя бы один город или `publication_city_id`).
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [x] Добавить проверку обязательности города в `validate()` и `hasCriticalErrors` на `app/employer/vacancies/page.tsx`
  - [x] Показ ошибки под блоком городов и блокировка отправки формы
- **Зависимости**: Актуальная схема `controllers.JobUpsertRequest` в `docs/swagger.yaml`

## Задача: Вакансии компании — список/деталь по Companies endpoints
- **Статус**: В процессе
- **Описание**: Получение списка вакансий через `GET /api/companies/jobs`, а при открытии редактирования — загрузка полной карточки через `GET /api/companies/jobs/{id}` (с предзагрузкой связей) согласно swagger.
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [ ] Добавить `companies.jobById(id)` в `API_ENDPOINTS`
  - [ ] Добавить `jobsApi.getCompanyJob(id)`
  - [ ] В `startEdit` загружать полную карточку и переинициализировать форму и мультивыборы
- **Зависимости**: Актуальный swagger, наличие Bearer токена

## Задача: Редактирование вакансии — предзаполнение мультивыборов
- **Статус**: В процессе
- **Описание**: При открытии редактирования вакансии заполнять мультивыборы из данных вакансии: тип занятости, уровень образования, формат работы, график работы, расписание, часов в день, смены. Поддержать форматы API (массивы ID и массивы сущностей).
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [ ] Инициализировать массивы `employment_type_ids`, `education_type_ids`, `work_format_ids`, `work_schedule_type_ids`, `work_schedule_ids`, `day_length_ids`, `shift_type_ids` в `startEdit`
  - [ ] Ручная проверка на нескольких вакансиях
- **Зависимости**: Структура ответа API вакансии (поддержка связей)

## Задача: Устранение наложения хедера на контент (перевод на sticky)
- **Статус**: В процессе
- **Описание**: Перевести `app/components/Header.tsx` с позиционирования `fixed` на `sticky` с `top-0 z-50`, чтобы хедер всегда оставался видимым при прокрутке и не перекрывал контент страниц (включая раздел `/employer/*`).
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [ ] Заменить `fixed` на `sticky` в `Header.tsx`
  - [ ] Провести визуальную проверку страниц работодателя на разных ширинах
- **Зависимости**: TailwindCSS классы, отсутствие конфликтующих глобальных стилей

## Задача: Ограничение доступа к /employer/vacancies
- **Статус**: Завершена
- **Описание**: Клиентская проверка роли `user_type === 'employer'` на странице `app/employer/vacancies/page.tsx`. При несоответствии выполняется редирект на `/login`.
- **Шаги выполнения**:
  - [x] Добавить проверку `localStorage` и редирект через `router.replace('/login')`
  - [x] Обновить документацию (changelog, plan)
- **Зависимости**: Наличие `localStorage.user_type`

## Задача: Страница профиля компании работодателя /employer/mycompany
- **Статус**: В процессе
- **Описание**: Реализовать страницу `/employer/mycompany`, которая загружает профиль компании текущего работодателя через `GET /api/companies/profile` и позволяет редактировать его поля по аналогии с формой добавления компании. Сохранение изменений выполняется через `POST /api/companies/profile`.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [ ] Добавить/уточнить типы `CompanyProfile` и DTO обновления профиля в `app/types/company.ts`
  - [ ] Доработать клиентский компонент `app/employer/mycompany/MyCompanyPageClient.tsx` (форма, загрузка и сохранение профиля)
  - [x] Улучшить UX выпадающего списка отраслей: применение по кнопке «Выбрать», закрытие по клику вне блока без применения
  - [x] Предзаполнение городов в адресах по `city_id` через `POST /api/dictionaries/cities-by-ids` для корректной работы `CityAutocomplete`
  - [ ] Обновить `docs/architecture.md` с описанием потока работы с профилем компании
- **Зависимости**: `GET/POST /api/companies/profile`, наличие Bearer токена, текущая реализация `/employer/addcompany`

## Задача: Синхронизация типов ID с документацией swagger
- **Статус**: В процессе
- **Описание**: Привести типы фронтенда к обновлённой схеме API: идентификаторы пользователей и связанные поля теперь строки (UUID). Затрагиваются: controllers.LoginResponse.user.user_id, models.JobPosting.posted_by, models.CompanyEmployee.user_id, models.CompanyProfile.user_id. 
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [ ] Обнови
<truncated 9028 bytes>
агрузка фото через `uploadFile(..., 'photo')`, отправлять `photo_url`=имя файла
  - [ ] `app/resume/[id]/page.tsx`: загрузка фото через `uploadFile(..., 'photo')`, обновлять резюме `photo_url`=имя файла
- **Зависимости**: Наличие публичной папки `public/uploads/photo`, корректный бэкенд-эндпоинт резюме

## Задача: Разработка системы коммуникации
- **Статус**: Не начата
- **Описание**: Реализация чата и системы уведомлений между пользователями
- **Шаги выполнения**:
  - [ ] Создание чат-комнат между соискателем и работодателем
  - [ ] Реальное время обмена сообщениями
  - [ ] Система уведомлений
  - [ ] Push уведомления в браузере
  - [ ] История переписки
  - [ ] Статус сообщений (прочитано/непрочитано)
  - [ ] Файловые вложения в чате
- **Зависимости**: Готовая система откликов на вакансии

## Задача: Интеграция с базой данных
- **Статус**: Не начата
- **Описание**: 
- **Шаги выполнения**:
  - [ ] Интеграция с Go API сервером
- **Зависимости**: Готовый Go API сервер

## Задача: Добавление фавиконок для JobFind
- **Статус**: В процессе
- **Описание**: Добавить две фавиконки (`/favicon-48x48.ico` и `/favicon-120x120.png`) и подключить их в корневом и клиентском лейаутах приложения для корректного отображения иконки сайта во всех браузерах.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [x] Добавить фавиконки в `metadata.icons` в `app/layout.tsx`
  - [x] Добавить теги `<link rel="icon">` в `app/client-layout.tsx`
  - [ ] Визуально проверить отображение иконок в браузере
- **Зависимости**: Наличие файлов `/public/favicon-48x48.ico` и `/public/favicon-120x120.png`

## Задача: Оптимизация производительности и SEO
- **Статус**: Частично завершена
- **Описание**: Улучшение производительности и поисковой оптимизации
- **Шаги выполнения**:
  - [x] Настройка Next.js App Router с оптимизацией
  - [x] Базовая SEO оптимизация страниц
  - [ ] Настройка ISR для динамических страниц
  - [ ] Оптимизация изображений и ресурсов
  - [ ] Настройка кеширования
  - [ ] Мониторинг производительности
  - [ ] Генерация sitemap и robots.txt
- **Зависимости**: Стабильная работа фронтенда

## Задача: Тестирование и развертывание
- **Статус**: Не начата
- **Описание**: Подготовка к продакшену и развертыванию
- **Шаги выполнения**:
  - [ ] Написание unit тестов
  - [ ] Интеграционные тесты API
  - [ ] E2E тестирование пользовательских сценариев
  - [ ] Настройка CI/CD pipeline
  - [ ] Контейнеризация с Docker
  - [ ] Развертывание на сервер
  - [ ] Настройка мониторинга и логирования
- **Зависимости**: Готовая полная функциональность

## Задача: Дополнительные функции и улучшения
- **Статус**: Не начата
- **Описание**: Расширение функциональности платформы
- **Шаги выполнения**:
  - [ ] Система рекомендаций вакансий
  - [ ] Расширенные фильтры поиска
  - [ ] Геолокация и поиск по регионам
  - [ ] Интеграция с соцсетями
  - [ ] Мобильное приложение (PWA)
  - [ ] Аналитическая панель для администраторов
  - [ ] Система рейтингов и отзывов
- **Зависимости**: Стабильная работа основной функциональности

## Задача: Страница откликов соискателя /my-applications
- **Статус**: Завершена
- **Описание**: Создать страницу для отображения списка откликов соискателя с фильтрацией по статусам и возможностью удаления откликов.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [x] Добавить endpoints в `app/config/api.ts` для получения откликов и обновления статуса
  - [x] Создать страницу `app/my-applications/page.tsx` с клиентским компонентом
  - [x] Реализовать фильтрацию по статусам с помощью чипсов (Все, Отправлено, Приглашение, Собеседование, Выход на работу, Ожидание, Отказ, Удалено, Архив)
  - [x] Добавить отображение количества откликов для каждого статуса
  - [x] Реализовать функционал удаления отклика (перевод в статус 'deleted') с модальным окном подтверждения
  - [x] Создать layout с Suspense для корректной работы useSearchParams
  - [x] Добавить проверку авторизации и роли пользователя (job_seeker)
- **Зависимости**: Эндпоинты `POST /api/applications/my`, `GET /api/applications/jobs/{job_id}/applicant-status-counts`, `PUT /api/applications/{application_id}/status`

## Примечания:
- Проект развивается поэтапно, начиная с фронтенда и постепенно добавляя бэкенд функциональность
- Текущий приоритет - завершение системы управления вакансиями и резюме
- Необходимо регулярное тестирование новых функций перед развертыванием
- Все изменения документируются в соответствии с правилами разработки

## Задача: Компонент пагинации для списков
- **Статус**: В процессе
- **Описание**: Реализовать UI-компонент пагинации (`app/components/Pagination.tsx`) для навигации по страницам списков вакансий.
- **Шаги выполнения**:
  - [ ] Создать компонент `Pagination` с пропсами `currentPage`, `totalPages`, `basePath`
  - [ ] Реализовать логику отображения диапазона страниц (например, 1 2 ... 5 6 7 ... 10)
  - [ ] Интегрировать в `app/vakansii/[slug]/page.tsx`
- **Зависимости**: Отсутствуют

## Задача: CI/CD деплой на VPS через GitHub Actions (e77.top)
- **Статус**: В процессе
- **Описание**: Настроить автоматический деплой Next.js-приложения на сервер Ubuntu в каталог /www/wwwroot/e77.top при пуше в main: сборка в Actions, упаковка артефактов, загрузка на VPS, установка зависимостей, перезапуск через pm2.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [ ] Обновить next.config.js: переписать rewrite /api на http://localhost:8081
  - [ ] Перевести API на относительные пути в app/config/api.ts: API_BASE_URL = ''
  - [ ] Добавить workflow .github/workflows/deploy.yml (сборка, архив, SCP, SSH, pm2 e77-top)
  - [ ] Настроить секреты репозитория: VPS_HOST, VPS_USER, VPS_SSH_KEY
  - [ ] Настроить Nginx: e77.top → 127.0.0.1:4000; выпустить сертификат Let's Encrypt
  - [ ] Тестовый деплой и проверка статуса процесса pm2 (e77-top)
    - [x] Временное ручное решение: код собран в `/www/wwwroot/e77.top`, каталог `/www/wwwroot/jobfind-fr` удалён после рестарта
- **Зависимости**: pm2 установлен на сервере; доступ по SSH-ключу; бэкенд доступен на localhost:8081

## Задача: Приведение формы вакансии к структуре БД
- **Статус**: В процессе
- **Описание**: Привести `app/employer/vacancies/page.tsx` и типы/пайлоады к таблице `public.job_postings`, добавить поля для связей (vacancy_day_lengths, vacancy_employment_types, vacancy_cities, vacancy_education_types, vacancy_regions, vacancy_shift_types, vacancy_work_formats, vacancy_work_schedule_types, vacancy_work_schedules), подгружать enum-значения из БД.
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [ ] Обнови
  - [x] Получить значения справочников из БД (employment_types, education_types, work_formats, work_schedule_types, work_schedules, work_day_lengths, shift_types) и сохранить в `app/config/*.json`
  - [x] Получить enum-значения (experience_level, work_experience, salary_type, salary_period, salary_frequency)
  - [ ] Обновить тип `JobPosting`/`JobUpsertPayload` и методы API для поддержки связанных полей (массивы id) и `work_experience`
  - [x] Обновить форму на `app/employer/vacancies/page.tsx`: добавить скролл модалки, убрать «Дата окончания публикации», заменить 6 полей на мультиселекты, добавить выпадающие списки для зарплаты
  - [x] Добавить мультиселект `work_schedules` и маппинг `*_ids`, `city_ids`, `region_ids` в `submitCreate/submitEdit`
  - [x] Добавить клиентскую валидацию (обязательные поля, диапазон зарплаты) и подсказки в форме
  - [x] Переключить `CityAutocomplete` на `/api/locations/cities/search`
  - [ ] Добавить поле «Опыт работы» (обязательное), RU-лейблы/EN-значения, закрытие по клику вне
  - [ ] Ручное тестирование create/update с новыми полями
- **Зависимости**: Строка подключения к PostgreSQL для MCP; поддержка бэкендом массивов ID и `work_experience` в payload вакансии

## Задача: Форма вакансии — «Адрес компании» (одиночный выбор)
- **Статус**: В процессе
- **Описание**: Заменить поле ввода ID на одиночный выпадающий список адресов компании, получаемых с `/api/companies/profile` (GET). В payload отправлять `company_address_id` (число), контракт бэкенда не меняется.
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [ ] Загрузить адреса компании из `/api/companies/profile`
  - [ ] Реализовать выпадающий список с одиночным выбором и установкой `company_address_id`
  - [ ] Обновить валидацию и UX (показ названия города в метке)
  - [ ] Ручное тестирование создания/редактирования вакансии
- **Зависимости**: Авторизация (Bearer), наличие адресов в профиле компании

## Задача: Форма вакансии работодателя — обработка ошибок загрузки и флаг loading
- **Статус**: В процессе
- **Описание**: Обновить функцию `load()` на странице `/employer/vacancies`, чтобы при ошибках API (`/api/companies/jobs`) состояние `loading` всегда корректно сбрасывалось, а пользователю отображалось понятное сообщение. Это предотвратит ситуацию, когда кнопка «Создать» навсегда задизейблена из-за зависшего `loading`.
- **Шаги выполнения**:
  - [x] Проанализировать причину зависания `loading` при ошибке API и влияние на кнопку «Создать»
  - [x] Сформировать и согласовать план фикса (обёртка `load()` в `try/catch/finally`)
  - [x] Обновить реализацию `load()` в `app/employer/vacancies/page.tsx` с корректной обработкой ошибок и финальным сбросом `loading`
  - [ ] Ручное тестирование: отключить бэкенд, убедиться, что появляется ошибка, но форма остаётся интерактивной, а кнопка «Создать» становится активной после заполнения полей
- **Зависимости**: Доступность `/api/companies/jobs`, корректная настройка прокси `/api` → `localhost:8081`

## Задача: Форма вакансии работодателя — адрес компании как необязательное поле
- **Статус**: В процессе
- **Описание**: Убрать обязательность поля `company_address_id` в форме создания и редактирования вакансии работодателя на `/employer/vacancies`. Кнопка «Создать/Сохранить» должна активироваться при корректном заполнении остальных обязательных полей, даже если адрес компании не выбран.
- **Шаги выполнения**:
  - [x] Согласовать изменение требований к адресу компании (поле становится опциональным)
  - [x] Удалить `company_address_id` из критической валидации `hasCriticalErrors` в `app/employer/vacancies/page.tsx`
  - [x] Удалить установку ошибки `company_address_id` в функции `validate()`
  - [ ] Ручное тестирование: создание/редактирование вакансии без выбранного адреса компании
  - [ ] Обновить подпись поля адреса компании в UI на "Адрес компании (необязательно)" для явного отражения опциональности

- **Зависимости**: Бэкенд должен корректно обрабатывать отсутствие `company_address_id` в payload вакансии

## Задача: CI/CD деплой на VPS — корректный запуск PM2 и каталог загрузок
- **Статус**: В процессе
- **Описание**: Обновить workflow `.github/workflows/deploy.yml`, чтобы PM2 запускал приложение из каталога `/www/wwwroot/e77.top`, а на сервере автоматически создавался каталог `public/uploads` для загрузок файлов.
- **Шаги выполнения**:
  - [x] Проанализировать текущий скрипт деплоя и причину запуска старой версии кода
  - [x] Согласовать решение: `pm2 delete` + `pm2 start npm --cwd "$DEPLOY_DIR" -- start -- -p 4000`
  - [ ] Добавить создание каталога `public/uploads/companyLogo` (и при необходимости других подпапок) в шаг деплоя
  - [ ] Ручное тестирование деплоя: убедиться, что процесс `e77-top` имеет корректный cwd и загрузка файлов работает
- **Зависимости**: Доступ к серверу VPS, установленный `pm2`

## Задача: Визуальный редактор описания вакансии работодателя
- **Статус**: В процессе
- **Описание**: Заменить текстовое поле описания вакансии на визуальный редактор (react-quill) с безопасной HTML-разметкой без гиперссылок и с санитизацией через DOMPurify перед отправкой.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan, architecture) под визуальный редактор описания
  - [ ] Добавить компонент `RichTextEditor` на базе `react-quill` + `DOMPurify` (без поддержки ссылок) и тему `quill.snow.css`
  - [ ] Сделать область ввода описания ресайзируемой (поддержка `resize-y`, минимальная высота, без визуальных артефактов)
  - [ ] Интегрировать редактор в `app/employer/vacancies/page.tsx` вместо `textarea` для `description`
  - [ ] Провести визуальную проверку и self-review формы создания/редактирования вакансии
- **Зависимости**: Пакеты `react-quill`, `dompurify`; сервер выполняет дополнительную санитизацию содержимого `description`

## Задача: Форма вакансии — единый UX мультивыборов
- **Статус**: В процессе
- **Описание**: Убрать обязательность поля `company_address_id` в форме создания и редактирования вакансии работодателя на `/employer/vacancies`. Кнопка «Создать/Сохранить» должна активироваться при корректном заполнении остальных обязательных полей, даже если адрес компании не выбран.
- **Шаги выполнения**:
  - [x] Согласовать изменение требований к адресу компании (поле становится опциональным)
  - [ ] Удалить `company_address_id` из критической валидации `hasCriticalErrors` в `app/employer/vacancies/page.tsx`
  - [ ] Удалить установку ошибки `company_address_id` в функции `validate()`
  - [ ] Ручное тестирование: создание/редактирование вакансии без выбранного адреса компании
- **Зависимости**: Бэкенд должен корректно обрабатывать отсутствие `company_address_id` в payload вакансии

## Задача: Форма вакансии — единый UX мультивыборов
- **Статус**: В процессе
- **Описание**: Обновить функцию `load()` на странице `/employer/vacancies`, чтобы при ошибках API (`/api/companies/jobs`) состояние `loading` всегда корректно сбрасывалось, а пользователю отображалось понятное сообщение. Это предотвратит ситуацию, когда кнопка «Создать» навсегда задизейблена из-за зависшего `loading`.
- **Шаги выполнения**:
  - [x] Проанализировать причину зависания `loading` при ошибке API и влияние на кнопку «Создать»
  - [x] Сформировать и согласовать план фикса (обёртка `load()` в `try/catch/finally`)
  - [ ] Обновить реализацию `load()` в `app/employer/vacancies/page.tsx` с корректной обработкой ошибок и финальным сбросом `loading`
  - [ ] Ручное тестирование: отключить бэкенд, убедиться, что появляется ошибка, но форма остаётся интерактивной, а кнопка «Создать» становится активной после заполнения полей
- **Зависимости**: Доступность `/api/companies/jobs`, корректная настройка прокси `/api` → `localhost:8081`

## Задача: Форма вакансии — единый UX мультивыборов
- **Статус**: В процессе
- **Описание**: Заменить нативные `<select multiple>` на выпадающие мультисписки с чекбоксами для полей: тип занятости, уровень образования, формат работы, график работы, расписание, часов в день, смены. Использовать источники из `app/config/*.json`.
- **Шаги выполнения**:
  - [x] Обновить документацию (changelog, plan)
  - [ ] Реализовать выпадающий мультивыбор для `employment_type_ids`
  - [ ] Реализовать выпадающий мультивыбор для `education_type_ids`
  - [ ] Реализовать выпадающий мультивыбор для `work_format_ids`
  - [ ] Реализовать выпадающий мультивыбор для `work_schedule_type_ids`
  - [ ] Реализовать выпадающий мультивыбор для `work_schedule_ids`
  - [ ] Реализовать выпадающий мультивыбор для `day_length_ids`
  - [ ] Реализовать выпадающий мультивыбор для `shift_type_ids`
- **Зависимости**: Источники справочников в `app/config/*.json`

## Примечания:
- Проект развивается поэтапно, начиная с фронтенда и постепенно добавляя бэкенд функциональность
- Текущий приоритет - завершение системы управления вакансиями и резюме
- Необходимо регулярное тестирование новых функций перед развертыванием
- Все изменения документируются в соответствии с правилами разработки
