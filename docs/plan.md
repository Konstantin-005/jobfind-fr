# План реализации фронтэнда платформы поиска работы JobFind

## Задача: Отключение предзагрузки контента на странице вакансий по городу
- **Статус**: Завершена
- **Описание**: Отключить автоматическую предзагрузку Next.js (prefetch={false}) для всех ссылок на странице `/vakansii/[slug]`, включая пагинацию и промо-ссылки, для снижения нагрузки на сервер и клиент.
- **Шаги выполнения**:
  - [x] Анализ ссылок в `page.tsx`, `Pagination.tsx` и `VacancySlugClient.tsx`
  - [x] Добавление `prefetch={false}` ко всем компонентам `Link`
  - [x] Актуализация документации
- **Зависимости**: `app/vakansii/[slug]/page.tsx`, `app/components/Pagination.tsx`

## Задача: Добавление поля «Ученический договор» (student_contract) в вакансии
- **Статус**: Завершена
- **Описание**: Добавить в DTO и UI создания/редактирования вакансии работодателем новое поле student_contract (Ученический договор).
- **Шаги выполнения**:
  - [x] Добавить поле в интерфейсы и состояние формы
  - [x] Добавить отправку поля при вызове create/update API
  - [x] Вывести чекбокс "Ученический договор" в блоке "Оформление"
- **Зависимости**: `app/employer/vacancies/_components/VacancyForm.tsx`

## Задача: Оптимизация производительности ключевых SSR страниц (вакансии и поиск по городу)
- **Статус**: Завершена
- **Описание**: Устранение дублирующих запросов и ускорение загрузки страниц /vakansii/[slug] и /vacancy/[id]
- **Шаги выполнения**:
  - [x] Анализ текущих запросов и выявление узких мест
  - [x] Внедрение react.cache для дедупликации запросов (город, вакансия)
  - [x] Параллелизация запросов через Promise.all (город + вакансии, вакансия + похожие)
  - [x] Актуализация документации
- **Зависимости**: API эндпоинты вакансий и городов

## Задача: Отображение названия вакансии в откликах
- **Статус**: Завершена
- **Описание**: Добавить отображение `job_title` в заголовке страницы для лучшего контекста вакансии, на которую откликнулся кандидат.
- **Шаги выполнения**:
  - [x] Добавить поле `job_title` в тип `Application`
  - [x] Обновить заголовок страницы для отображения названия вакансии
  - [x] Актуализировать документацию (changelog)
- **Зависимости**: `app/employer/vacancies/[id]/applications/VacancyApplicationsPageClient.tsx`

## Задача: Публичные страницы компаний
- **Статус**: В процессе
- **Описание**: Реализовать публичный каталог компаний `/companies` и страницу компании `/companies/{id}` с SSR-первой загрузкой и дальнейшей фильтрацией/пагинацией через query-параметры и публичные эндпоинты словарей. Для каталога: поддержка поиска (`query`), фильтра по отраслям (`industry_id[]`), серверной пагинации (`page`, `limit`) и SEO запрета индексации для страниц с `query` и/или `page > 1`.
- **Шаги выполнения**:
  - [x] Реализовать SSR-страницу `/companies` с первичной загрузкой списка компаний и отраслей
  - [x] Реализовать SSR-страницу `/companies/{id}` с вкладками «Вакансии» и «Инфо»
  - [x] Настроить отображение 404-страницы «Компания не найдена» для несуществующих компаний
  - [ ] Перевести каталог `/companies` на серверную пагинацию `page`/`limit` с форматом ответа `items/total/page/limit`
  - [ ] Синхронизировать состояние поиска/фильтра по отраслям с URL (`query`, `industry_id[]`), при смене фильтров сбрасывать `page=1`
  - [ ] Добавить SEO запрет индексации (robots + Yandex) для `/companies` при наличии `query` и/или `page > 1` (canonical на базовую страницу)
- **Зависимости**: `docs/swagger.yaml` (эндпоинты `/api/dictionaries/companies` и `/api/dictionaries/companies/{id}`), `app/config/work_formats_202505222228.json`

## Задача: Чат — безопасный рендер HTML сообщений
- **Статус**: Завершена
- **Описание**: Обеспечить отображение входящих сообщений с преформатированной HTML-разметкой через очистку и безопасный рендер в списке комнат и ленте переписки.
- **Шаги выполнения**:
  - [x] Добавить очистку HTML сообщений через DOMPurify с whitelisted-тегами и атрибутами
  - [x] Применить безопасный рендер в превью последнего сообщения списка комнат
  - [x] Применить безопасный рендер в ленте сообщений активного чата
- **Зависимости**: `app/components/ChatInterface.tsx`, DOMPurify

## Задача: Отклики — переход на разделённые поля ФИО
- **Статус**: Завершена
- **Описание**: Адаптировать фронтенд страницы откликов под новый ответ бекэнда без поля `full_name`, используя `first_name`, `last_name`, `middle_name` для отображения ФИО и генерации сообщений.
- **Шаги выполнения**:
  - [x] Обновить тип `Application` под новые поля имени
  - [x] Формировать и выводить ФИО в карточках и модалке смены статуса
  - [x] Убрать обращения к `full_name` в UI и логике сообщений
- **Зависимости**: `app/employer/vacancies/[id]/applications/VacancyApplicationsPageClient.tsx`

## Задача: Контакты резюме — переход на строковые поля WhatsApp/Telegram
- **Статус**: В процессе
- **Описание**: Обновить фронтенд под новое API контактов резюме: заменить флаги `has_whatsapp`/`has_telegram` на строковые поля `whatsapp`/`telegram` во всех запросах и ответах, не ломая текущий UX форм и списка откликов.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan) под изменение модели контактов резюме
  - [ ] Обновить типы и DTO откликов/резюме на фронтенде (использовать `whatsapp`/`telegram` вместо `has_whatsapp`/`has_telegram`)
  - [ ] Обновить запросы создания и обновления резюме (JSON и multipart) на использование строковых полей `whatsapp`/`telegram`
  - [ ] Обновить чтение и отображение контактов в списке откликов работодателя `/employer/vacancies/[id]/applications`
  - [ ] Провести ручное тестирование сценариев создания/редактирования резюме и просмотра откликов
- **Зависимости**: эндпоинты `/api/resumes`, `/api/resumes/{id}`, `/api/jobs/{job_id}/applications`, файлы `app/user/resume/add/page.tsx`, `app/user/resume/[id]/page.tsx`, `app/employer/vacancies/[id]/applications/VacancyApplicationsPageClient.tsx`

## Задача: Управление вакансиями работодателя — просмотр откликов и переход к резюме
- **Статус**: В процессе
- **Описание**: Улучшить UX страницы просмотра откликов работодателя, добавив прямой переход к публичному резюме соискателя из карточки отклика и привести оформление текста и иконки к единому, минималистичному стилю.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan) под изменение страницы `/employer/vacancies/[id]/applications`
  - [x] Обновить `VacancyApplicationsPageClient`: сделать `resume_title` ссылкой на `/resume/[link_uuid]` с открытием в новой вкладке и добавить справа иконку стрелки вверх-вправо
  - [x] Унифицировать цвет текста в блоке основной информации отклика (название резюме, ФИО, опыт, контакты) до чёрного
  - [x] Упростить иконку ссылки на резюме, убрав лишние линии и оставив только диагональную стрелку вверх-вправо
  - [ ] Провести ручное тестирование перехода по ссылке на резюме из списка откликов и визуально проверить новый стиль текста и иконки
- **Зависимости**: `app/employer/vacancies/[id]/applications/VacancyApplicationsPageClient.tsx`, маршрут `/resume/[link_uuid]`

## Задача: Модалка смены статуса отклика с сообщением
- **Статус**: В процессе
- **Описание**: Добавить модальное окно выбора статуса отклика с отображением ФИО, заголовка резюме, ссылкой на резюме, полем сообщения с автоподстановкой текста и названием компании, чекбоксом «Отправить сообщение», вызовами PUT `/api/applications/{application_id}/employer-status` и `/api/applications/bulk-employer-status`.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan) под новую модалку смены статуса
  - [ ] Реализовать модалку смены статуса и интегрировать её в список откликов
  - [ ] Подключить тексты по статусам и подстановку названия компании
  - [ ] Обновить вызовы API одиночного и массового изменения статуса с передачей сообщения
  - [ ] Провести ручное тестирование изменения статуса и отправки сообщений
- **Зависимости**: `app/employer/vacancies/[id]/applications/VacancyApplicationsPageClient.tsx`, `app/components/JobOfferModal.tsx`, эндпоинты `/api/applications/{application_id}/employer-status`, `/api/applications/bulk-employer-status`

## Задача: Поиск резюме — фильтрация по локациям
- **Статус**: В процессе
- **Описание**: Обеспечить корректную работу фильтра по регионам и городам в поиске резюме для работодателя на основе единого эндпоинта `/api/locations`.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan) под изменение фильтра локаций в модальном окне поиска резюме
  - [x] Обновить `ResumeFiltersModal` для корректного парсинга ответа `/api/locations` (поля `id`, `type` → `city_id`/`region_id`)
  - [ ] Провести ручное тестирование выбора и удаления локаций в модалке фильтров поиска резюме и убедиться, что значения попадают в запрос
- **Зависимости**: `app/components/ResumeFiltersModal.tsx`, эндпоинт `/api/locations`, клиентская страница поиска резюме `/resume`


## Задача: Поиск резюме — быстрые фильтры
- **Статус**: В процессе
- **Описание**: Реализовать быстрые фильтры на странице `/resume`, связанные с теми же полями фильтрации, что и в `ResumeFiltersModal`, с подсветкой активных состояний и отдельными модальными окнами по каждой группе параметров.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan) под быстрые фильтры поиска резюме
  - [ ] Реализовать быстрые фильтры в `ResumeSearchPageClient.tsx` (подсветка активных и открытие отдельной модалки по каждому быстрому фильтру с синхронизацией с общей)
  - [ ] Провести ручное тестирование работы быстрых фильтров совместно с модальным окном фильтров
- **Зависимости**: `app/resume/ResumeSearchPageClient.tsx`, `app/components/ResumeFiltersModal.tsx`, эндпоинт поиска резюме

## Задача: Чат — блокировка отправки при отказе работодателя
- **Статус**: Завершена
- **Описание**: Запретить отправку новых сообщений в чате, если поле `employer_status` в ответе `/api/chat/messages/{id}` равно `rejected`, заблокировав ввод и кнопку отправки.
- **Шаги выполнения**:
  - [x] Актуализировать changelog и план под новое поведение чата
  - [x] Обновить типы и загрузку сообщений для сохранения `employer_status`
  - [x] Заблокировать поле ввода и кнопку отправки при статусе `rejected`
- **Зависимости**: `app/components/ChatInterface.tsx`, `app/types/chat.ts`, эндпоинт `/api/chat/messages/{id}`

## Задача: Предложение вакансии по резюме
- **Статус**: В процессе
- **Описание**: Реализовать для работодателя возможность отправлять предложение по своей вакансии конкретному резюме как с публичной страницы резюме, так и из списка резюме, с обработкой ошибок авторизации, валидации и дубликатов, а также с интеграцией с модулем чатов.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan, architecture) под новый флоу «Предложить вакансию»
  - [ ] Добавить кнопку и модалку выбора вакансии на публичной странице резюме `/resume/[link_uuid]` с вызовом `POST /api/jobs/{job_id}/offer`
  - [ ] Добавить возможность отправлять предложение из списка резюме `/resume` (через `ResumeSearchPageClient` и `ResumeCard`) с повторным использованием той же модалки
  - [ ] Обработать основные статусы ошибок (401, 403, 400, 404, 500) в соответствии с UX-описанием и добавить переход в чат при наличии `chat_room_id`
- **Зависимости**: `app/resume/[link_uuid]/page.tsx`, `app/resume/ResumeSearchPageClient.tsx`, `app/components/ResumeCard.tsx`, `app/utils/api.ts`, `app/config/api.ts`, модуль чатов (`/chat`)

## Задача: Скрытие деталей соискателя при массовом отказе
- **Статус**: Завершена
- **Описание**: При клике на "Отказать всем" в модальном окне не должно отображаться ФИО соискателя, ссылка на его резюме, а выбор статуса должен быть ограничен одним вариантом – "Отказ".
- **Шаги выполнения**:
  - [x] Скрыть ФИО и ссылку на резюме при bulk-действии
  - [x] Ограничить варианты селекта статусов при bulk-действии
  - [x] Актуализировать документацию (changelog, plan)
- **Зависимости**: `app/employer/vacancies/[id]/applications/VacancyApplicationsPageClient.tsx`

## Задача: Блок "Работа в других регионах" на странице вакансий по городу
- **Статус**: Завершена
- **Описание**: Добавить блок со ссылками на соседние города региона в нижней части страницы `/vakansii/[slug]` над футером. Загрузка данных через эндпоинты `/api/dictionaries/cities/by-slug/{slug}` (для получения ID города) и `/api/dictionaries/cities/{id}/neighbors` (для получения списка соседних городов с параметрами `population=20000` и `limit=10`).
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [x] Получить ID города из slug через `/api/dictionaries/cities/by-slug/{slug}`
  - [x] Загрузить соседние города через `/api/dictionaries/cities/{id}/neighbors?population=20000&limit=10`
  - [x] Реализовать UI блока с заголовком "Работа в других регионах" и кнопками-ссылками на города
  - [x] Провести ручное тестирование отображения блока и переходов по ссылкам
- **Зависимости**: `app/vakansii/[slug]/page.tsx`, эндпоинты `/api/dictionaries/cities/by-slug/{slug}` и `/api/dictionaries/cities/{id}/neighbors`

## Задача: Добавление карты на страницу вакансии (SSR)
- **Статус**: Завершена
- **Описание**: Отображение точки вакансии на карте OpenStreetMap с использованием pigeon-maps и получение точного адреса по координатам через обратное геокодирование Nominatim на стороне сервера (SSR).
- **Шаги выполнения**:
  - [x] Установка библиотеки pigeon-maps
  - [x] Обновление интерфейсов для поддержки координат (latitude, longitude)
  - [x] Реализация серверной функции `getReverseGeocode` для получения адреса на этапе SSR
  - [x] Создание клиентского компонента VacancyMap для отображения карты
  - [x] Интеграция карты на страницу вакансии с передачей готового адреса
  - [x] Актуализация документации (changelog, plan)
- **Зависимости**: `app/vacancy/[id]/page.tsx`, `pigeon-maps`

## Задача: Блок "Похожие вакансии" на странице детальной вакансии
- **Статус**: В процессе
- **Описание**: Добавить блок с похожими вакансиями на странице `/vacancy/[id]`. После загрузки основной вакансии фронт запрашивает `GET /api/jobs/{job_id}/similar?n=6`. Ответ содержит `variant` (profession/company) и `items`. Заголовок блока зависит от variant: «Похожие вакансии» или «Другие вакансии компании». Блок не блокирует загрузку страницы, загружается отдельно. При пустом items блок не показывается.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [ ] Создать клиентский компонент `SimilarJobs` с загрузкой и отображением похожих вакансий
  - [ ] Интегрировать компонент в страницу `/vacancy/[id]`
  - [ ] Провести ручное тестирование
- **Зависимости**: `app/vacancy/[id]/page.tsx`, эндпоинт `GET /api/jobs/{job_id}/similar`

## Задача: Адаптация страницы вакансии под новый формат GET /api/jobs/{job_id}
- **Статус**: В процессе
- **Описание**: Обновить SSR-страницу детальной вакансии `/vacancy/[id]` под новый формат ответа API (включая обновлённый `company_profile`) без деградации SEO и схемы JobPosting.
- **Шаги выполнения**:
  - [x] Актуализировать документацию (changelog, plan)
  - [ ] Обновить типы и чтение локации (`addresses`) на странице `/vacancy/[id]`
  - [ ] Провести ручную проверку рендера (зарплата, компания, адрес, карта, метаданные)
- **Зависимости**: `app/vacancy/[id]/page.tsx`, эндпоинт `GET /api/jobs/{job_id}`

